# ⚠ 這個檔案一定要留著
#
# 你之前已經把網站上傳過一次 GitHub，而現在這個資料夾裡有一個
# 絕對不能公開的檔案。有這個 .gitignore，git 就不會把它加進版控、
# 也就不可能被推上 GitHub 或部署到 Cloudflare。

# 內含你的 Steam Web API 金鑰
steam-api-key.txt

# 你原本給我的素材。裡面的檔案都已經複製到 images/、media/ 底下，
# 留著只是重複佔空間（影片 7.4 MB + 音樂 56 MB），沒有必要上傳。
# 用萬用字元涵蓋 image / music 兩個資料夾。
user provide image/
user provide music/

# 小說的原始 .txt 檔。
# 網站實際上讀的是 novels.js（已經轉好繁體、整理成程式可讀的格式），
# context/ 只是你的原稿。兩份都上傳的話，以後會搞不清楚改哪邊才有效，
# 所以這裡排除，只留 novels.js。
# 改了 .txt 之後要重新產生 novels.js —— 跟我說一聲就好。
context/

# 作業系統與編輯器雜項
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
